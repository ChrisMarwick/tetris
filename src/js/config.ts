export const NUM_ROWS = 20;
export const NUM_COLUMNS = 10;
export const CELL_SIZE = 50;

export const GRID_WIDTH = NUM_COLUMNS * CELL_SIZE
export const GRID_HEIGHT = NUM_ROWS * CELL_SIZE

export const INFO_BAR_X = GRID_WIDTH;
export const INFO_BAR_WIDTH = 200;
export const INFO_BAR_HEIGHT = NUM_ROWS * CELL_SIZE;

export const CANVAS_WIDTH = GRID_WIDTH + INFO_BAR_WIDTH;
export const CANVAS_HEIGHT = GRID_HEIGHT;